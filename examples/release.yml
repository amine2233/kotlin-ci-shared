# Copy into the consumer repo at: .github/workflows/release.yml
# Requires a mise.toml with `release` and `publish` tasks (see templates/mise.toml)
# and a .releaserc.json (see templates/.releaserc.json).
#
# Manual by default (Actions → Release → Run workflow). To release automatically
# on every push to main, set the repository variable AUTO_RELEASE=true.
name: Release

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  ci:
    if: ${{ github.event_name == 'workflow_dispatch' || vars.AUTO_RELEASE == 'true' }}
    uses: amine2233/kotlin-ci-shared/.github/workflows/ci.yml@1.0.0

  release:
    needs: ci
    permissions:
      contents: write
      packages: write
      issues: write
      pull-requests: write
    uses: amine2233/kotlin-ci-shared/.github/workflows/semantic-release.yml@1.0.0
    secrets: inherit
